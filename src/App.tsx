function App() {
  return (
    <main
      style={{
        minHeight: '100vh',
        padding: 'var(--page-padding)',
      }}
    >
      <h1
        style={{
          fontSize: 'var(--font-size-3xl)',
        }}
      >
        Yarl OG
      </h1>

      <p
        style={{
          marginTop: 'var(--space-4)',
          color: 'var(--color-text-muted)',
        }}
      >
        Robinhood Chain
      </p>
    </main>
  )
}

export default App